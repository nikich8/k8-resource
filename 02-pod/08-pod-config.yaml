apiVesion: v1
kind: Pod
metadata:
  name: config-pod
spec:
  containers:
  - name: config-pod
    image: nginx
    ports:
    - containerPort: 80
    env:
    - name: course
      valueFrom:
        configMapkeyRef:
          name: devops-config
          key: course
    - name: trainer
      valueFrom:
        configMapkeyRef:
          name: devops-config
          key: trainer

