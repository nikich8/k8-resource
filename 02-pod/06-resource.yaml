apiVersion: v1
Kind: Pod
metadate:
  name: hello-pod
spec:
 containers:
  - name: hello-pod
    image: nginx
    ports:
    - conterPorts: 80
    resources:
      request:
        cpu: "100m"
        memory: "68Mi"
      limits:
        cpu: "200m"
        memory: "128Mi"
          
